machine:
  environment:
    ANDROID_HOME: /home/ubuntu/android
  java:
    version: oraclejdk8

dependencies:
  cache_directories:
    - ~/.android
    - ~/android

  override:
    - sh ./install-dependencies.sh
    - ./gradlew dependencies

test:
  override:
   - $ANDROID_HOME/tools/emulator -avd testing -no-window -no-boot-anim -no-audio:
      background: true
      parallel: true
   - bash ./wait.sh:
      parallel: true
   - ./gradlew mvp-test:build mvp-test:jacocoTestReport
